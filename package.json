{
  "name": "travel-agent",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "precommit": "npm run test",
    "preship": "npm run test && npm run build",
    "ship": "lerna publish --conventional-commits",
    "release": "standard-version",
    "test": "lerna run test"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lerna": "^2.10.2"
  },
  "devDependencies": {
    "husky": "^0.14.3",
    "standard-version": "^4.3.0"
  }
}
